{
  "name": "peco-backend-layer",
  "version": "1.0.0",
  "description": "AWS Lambda Layer containing all dependencies of Peco backend lambdas",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-status-codes": "^2.3.0",
    "short-uuid": "^4.2.2",
    "uuid": "^9.0.1",
    "uuid-apikey": "^1.5.3"
  }
}
